export enum ApiSourceAuthMethod {
  BASIC = "basic",
  BEARER = "bearer",
  X_API_KEY = "x-api-key",
  NONE = "none",
}

export enum ApiProdutsUpdateInterval {
  DAILY = "Daily",
  WEEKLY = "Weekly",
  MONTHLY = "Monthly",
  NEVER = "Never",
}
